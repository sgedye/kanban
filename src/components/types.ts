export interface TaskInterface {
  id: string;
  title: string;
  description?: string;
  // project: string;
  // status: string;
  // priority: "High" | "Medium" | "Low";
  // createdAt: string;
  // lastModifiedAt: string;

}